package com.example.drivingschool.ui.fragments.main.lesson

enum class LessonStatus(val status: String) {
    PLANNED("planned"), FINISHED("finished"), ACTIVE("active"), CANCELED("cancelled"), NOTCOME("notCome")
}